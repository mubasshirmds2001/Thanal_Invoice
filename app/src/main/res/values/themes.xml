<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Thanal_Invoice" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/thanal_blue</item>
        <item name="colorPrimaryVariant">@color/thanal_blue</item>
        <item name="colorAccent">@color/thanal_blue</item>

        <!-- Background color -->
        <item name="android:colorBackground">@color/light_gray_page</item>

        <!-- Surface/Card color -->
        <item name="colorSurface">@color/white_card</item>

        <!-- Global text color -->
        <item name="android:textColor">@color/dark_slate_gray</item>

    </style>
    <!-- Base application theme. -->
    <style name="Base.Theme.Thanal_Invoice" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="MyButton" parent="Widget.MaterialComponents.Button">
        <item name="android:textColor">@color/white_button_text</item>
        <item name="backgroundTint">@color/thanal_blue</item>
    </style>

</resources>